<%= form_for(@ope_kind) do |f| %>
  <% if @ope_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ope_kind.errors.count, "error") %> prohibited this ope_kind from being saved:</h2>

      <ul>
      <% @ope_kind.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br />
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
